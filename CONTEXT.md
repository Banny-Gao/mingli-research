# CONTEXT — 领域词汇表

> 本文件只记录领域术语与含义,不含实现细节。完整设计与 ADR 见 `docs/adr/`。

## 短剧创作 (Short Drama Production)

| 术语 | 定义 |
|---|---|
| **短剧** | 竖屏微短剧,单集 1-5 分钟,平台为抖音/快手/红果等。 |
| **整体框架 (Series Framework)** | 一部剧的高层设计:选题、核心冲突、主角设定、分集主线、目标受众、集数规划。每部剧一份,是后续所有集创作的唯一依据。 |
| **单集 (Episode)** | 一部剧中的一集,包含剧本、分镜、资产、镜头、证据。 |
| **剧本 (Script)** | 一集的完整叙事文本:场景块、台词、情绪标记、镜头建议。 |
| **分镜表 (Storyboard)** | 从剧本拆解的镜头清单,每镜含镜号/场景/人物/动作/台词/镜头语言/时长/模式/优先级。 |
| **镜头 (Shot)** | 分镜表中的最小出片单位,对应一次 H3 视频生成。 |
| **角色圣经 (Character Bible)** | 一角色的视觉一致性资产:多角度参考图、表情锚、服装锚 + 人设描述。 |
| **声音锚 (Voice Anchor)** | 一角色的声音一致性资产:用固定 voice_id 的 TTS 生成的 2-3 段标准对白音频。 |
| **资产 (Asset)** | 角色/场景/道具的参考图或音频,用于锁角色一致性。 |
| **本剧共享资产 (Series Assets)** | 一部剧内跨集复用的资产,存于本剧 `assets/`。 |
| **创作证据 (Evidence)** | 创作过程的结构化记录(log.md + log.jsonl):每次产出的产出物、关键参数、决策理由、版本。用途是复盘回放。 |
| **补丁 (Patch)** | 编辑模式:创作者改动剧本/分镜后,只重生成受影响的下游环节。 |
| **前置校验 (Pre-flight Validation)** | 出片前的自动规则检查 + 人工 gate,用于降低单条视频重做率。 |
| **成片验收 (Shot Acceptance)** | 生成后抽帧,LLM 对比参考图审 + 人工确认,PASS/FAIL 判定。 |

## H3 生成模式 (MiniMax H3 Modes)

| 术语 | 定义 |
|---|---|
| **T2V (t2va)** | 文生视频:仅文本提示词。用于空镜/远景。 |
| **I2V (i2va)** | 图生视频:文本 + 首帧/尾帧图。首帧图决定初始画面。 |
| **R2V (r2va)** | 多模态参考生视频:文本 + 参考图(≤9) + 参考视频(≤3) + 参考音频(≤3)。用于锁脸/锁声/动作接续。`reference_*` 与 `first_frame/last_frame` 互斥。 |
| **Context-IR** | H3 提示词增强接口:分析多模态素材输出三段式增强提示词(分镜/环境音/配乐),不生成视频,按 token 计费。 |
| **Regeneration** | 768P 成片再生成 2K(须原样提交原始输入+最终 prompt)。 |
| **声音锚双锁** | 每镜用 r2va:参考图锁脸 + 参考音频锁声,保证跨镜角色一致。 |

## 共享资源 (Shared Resources)

| 术语 | 定义 |
|---|---|
| **共享注册表 (Shared Manifest)** | 跨剧共享资产的登记表(资产名→路径/描述/适用剧)。v1 仅约定,不强机制。 |
