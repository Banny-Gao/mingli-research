// Auto-generated — do not edit manually
export default `---
name: zhonghe
displayName: 中和平衡评估器
type: analysis
input: 八字四柱（年、月、日、时柱的天干地支）及前期衰旺判定结果
output: 中和状态评估报告
description: 基于滴天髓中和篇的不偏不倚理论，评估日主与用神之间的平衡程度，并给出扶抑策略建议
---

## 功能

在完成日主衰旺判定后，评估命局是否达到"中和"状态——即日主与用神之间是否"相停"（力量平衡）。如果失衡，给出扶抑策略建议（扶弱抑旺）。此技能是对衰旺判定结果的应用和深化，为用神选取提供"扶"或"抑"的方向性指导。

## 输入

- **四柱八字**：年柱、月柱、日柱、时柱对应的天干地支（字符串，格式如"甲子 丙寅 戊午 庚申"）
- **日主衰旺判定结果**：来自衰旺判定器的输出（日主等级：极旺/偏旺/中和/偏弱/极弱）
- **性别**：乾造（男）或坤造（女）（可选）

## 处理逻辑

1. **日主状态确认**
   a. 接收日主衰旺判定结果
   b. 确认日主在当前命局中的绝对强弱
   c. **依据**：中和篇【任氏曰】"日主中和，则无太过不及之患"

2. **用神状态评估**
   a. 初步判断可能的用神（与日主相停之神）
   b. 评估用神在命局中是否"得力"：
      - 用神有根吗？
      - 用神被冲克吗？
      - 用神被合绊吗？
   c. **依据**：中和篇【原句】"日主中和，用神得力，富贵寿考"

3. **日主与用神比例判定**
   a. 判断日主与用神之间是否"相停"（力量对等）
   b. **依据**：中和篇【原注】"日主与用神相停，无过无不及"

4. **扶抑策略生成**
   a. 如果日主偏旺 → 用克泄（官杀制、食伤泄、财耗）
   b. 如果日主偏弱 → 用生扶（印星生、比劫帮）
   c. 如果日主极端（极旺/极弱）→ 判断是否适合走从格
   d. 如果日主中和 → 随运而行，不需要人为强加扶抑
   e. **依据**：中和篇【任氏曰】"用神得力，则扶抑有功"

5. **命格等级判定**
   a. 判断是否为"上格"（中和+用神得力）
   b. **依据**：中和篇【原句】"此命之上格也"

## 输出

\`\`\`typescript
interface ZhongHeReport {
  /** 日主状态 */
  dayMaster: {
    stem: string;
    element: string;
    level: "极旺" | "偏旺" | "中和" | "偏弱" | "极弱";
  };
  /** 用神状态 */
  usefulGod: {
    candidate: string[];        // 候选用神列表
    isEffective: boolean;       // 是否得力
    rootStatus: "有根" | "少根" | "无根";
    conflict: string[];         // 冲克合绊等不利因素
  };
  /** 平衡状态 */
  balance: {
    isBalanced: boolean;        // 是否"相停"
    assessment: "中和" | "偏颇";
    description: string;        // 对日主与用神比例的中文描述
  };
  /** 扶抑策略 */
  strategy: {
    direction: "扶" | "抑" | "从" | "顺行";
    method: string[];           // 具体方法（如"官杀制","印星生"）
    target: string;             // 目标状态
  };
  /** 命格等级 */
  grade: {
    level: "上格" | "中格" | "下格" | "特殊格局";
    isTopGrade: boolean;        // 是否为"此命之上格"
    keyInsight: string;         // 基于中和篇的关键发现
  };
}
\`\`\`
`;
