# 字形策略维度与紫微斗数全书繁转简

> **日期：** 2026-06-08
> **状态：** 待用户审阅
> **目标：**
> 1. 在 `SPEC-source.md` 引入"字形策略"维度，让不同典籍可选不同录入口径
> 2. 对 `紫微斗数全书` 实施 `简体规范化` 整理（目录与正文同步繁→简）
> 3. 对 `渊海子平`、`滴天髓阐微` 显式声明 `原文照录`

---

## 一、问题与动机

`紫微斗数全书` 100+ 篇章的 source.md 在繁简上存在混用，无统一规则：

**目录命名层：**

- 繁：`問紫微所主若何？`、`太微賦`、`形性賦`、`星垣論`、`斗數準繩`、`斗數發微論`、`重補斗數彀率`、`增補太微賦`
- 简：`问化忌星所主若何？`、`兄弟`、`十二宫诸星失陷破格诀`、`父母`、`财帛`

**正文层：**

- 全繁：`太微賦/source.md`（"斗數至玄至微"、"諸星"、"帝座"）
- 全繁：`問紫微所主若何？/source.md`（"紫微屬土"、"主掌"、"諸宮"）
- 全简：`兄弟/source.md`（"倚靠"、"和睦"、"分居"）

**根本原因：** 现行 `SPEC-source.md §2.1` 严令"只录入原文，不改变任何一个字"，对繁简混用没有处理空间；同时没有"按书声明录入口径"的元数据维度。

**目标：**

1. SPEC 引入"字形策略"维度（`原文照录` | `简体规范化`），允许不同书选不同口径
2. 紫微斗数全书 改为 `简体规范化` 并实际转换
3. 渊海子平、滴天髓阐微 显式声明 `原文照录`，内容不动

---

## 二、SPEC 变更：`SPEC-source.md` 新增"字形策略"节

在 §二 录入原则 之后新增 §X。完整草稿：

```markdown
## X、字形策略

> **声明位置：** `books/{slug}/catalog.md` 元信息 blockquote 中
> **取值：** `原文照录`（默认） | `简体规范化`
> **作用域：** 该书 `source.md` 内容、目录名、`# 标题` 行、`catalog.md` 表格篇名

### X.1 两种策略

| 策略 | 含义 | 与 §2.1 关系 |
|------|------|-------------|
| `原文照录`（默认） | 1:1 对应印本，严字不改 | §2.1 全效生效 |
| `简体规范化` | 允许繁→简机械转换 + 歧义字语义复核 | §2.1 的"不改变任何一个字"对该策略做例外放行 |

### X.2 `原文照录` 工作流（默认）

§2.1 完全生效。异体字、避讳字、缺笔字、繁简差异字 一律照录原字形。适合版本有定本、校勘价值高的典籍。

### X.3 `简体规范化` 工作流

1. **机械转换**：使用标准 t2s 映射表（推荐 OpenCC `t2s`）对 source.md 逐字转换
2. **歧义字清单与复核**：LLM 列出歧义字映射清单初稿，人工 spot-check 关键出现处（按文意判定）：
   - `復` → `复`（重复）/ `覆`（覆盖）/ `複`（复杂）— 依语境
   - `像` → `像`（图像）/ `象`（象征/象棋）
   - `干` → `干`（干涉）/ `幹`（干部）/ `乾`（乾坤）
   - `后` → `后`（皇后/后面）/ `後`（之後）
   - `台` → `台` / `臺`（臺灣）
   - `余` → `余`（剩余）/ `餘`
   - `里` → `里`（公里）/ `裏`（裏面）
   - `几` → `几`（几个）/ `幾`（幾乎）
3. **目录与标题同步**：目录名、`# 篇名` 走同一转换
4. **catalog.md 同步**：表格篇名更新，元信息加 `字形策略：简体规范化`
5. **可追溯**：整本书转换合并为单次 commit（commit message 注明 `feat(紫微斗数全书): 繁转简规范化`），便于审查与回滚

### X.4 标注方式

在 `books/{slug}/catalog.md` 的元信息 blockquote 中以一行声明：

​```markdown
> 字形策略：简体规范化
​```

未声明则视为 `原文照录`。`渊海子平`、`滴天髓阐微` 这次显式补上 `字形策略：原文照录`，避免后人误读为"默认可改"。

### X.5 与下游规范的关系

- `SPEC-interpretation.md`：解读时如需引用原文，从 source.md 现状引用，**不再回溯**到原印本繁简
- `generate.js`：路径推导基于 `articles/{篇名}/`，目录名变化由其自适应，无需改动
- `search-index.json`：重新生成即可，自动反映字形变化
```

---

## 三、落地动作

### 3.1 紫微斗数全书（实施 `简体规范化`）

| 步骤 | 内容 | 数量 |
|------|------|------|
| 1 | 编写/选择标准 t2s 映射表（OpenCC `t2s`） | 1 |
| 2 | 对 100+ 个 source.md 做繁→简机械转换 | 100+ 文件 |
| 3 | 歧义字清单 + 全文语境化复核（人工/辅助） | 100+ 文件 |
| 4 | 目录 `git mv` 重命名 | 100+ 目录 |
| 5 | 每个 source.md 的 `# 标题` 行同步 | 100+ 文件 |
| 6 | 更新 `catalog.md` 元信息 + 篇名表 | 1 文件 |
| 7 | 编写校验脚本：检查是否还有高频繁体残留 | 1 脚本 |
| 8 | 单次 commit 提交（与 §X.3 第 5 条一致） | 1 commit |

**机械转换覆盖的典型字符（举例，非穷举）：**

`數→数`、`體→体`、`學→学`、`會→会`、`經→经`、`過→过`、`還→还`、`進→进`、`與→与`、`說→说`、`請→请`、`視→视`、`雜→杂`、`響→响`、`導→导`、`記→记`、`應→应`、`單→单`、`當→当`、`條→条`、`長→长`、`聲→声`、`實→实`、`機→机`、`頭→头`、`義→义`、`觀→观`、`為→为`、`處→处`、`東→东`、`難→难`、`廣→广`、`歷→历`、`顯→显`、`證→证`、`龍→龙`、`備→备`、`黨→党`、`斷→断`、`嚴→严`、`邊→边`、`屬→属`、`陰→阴`、`陽→阳`、`陰→阴`

### 3.2 渊海子平、滴天髓阐微（仅声明）

仅在 `catalog.md` 元信息中显式补：

​```markdown
> 字形策略：原文照录
​```

source.md 内容不动。

### 3.3 SPEC 文档

将 §X 字形策略 节写入 `research-dispute/SPEC-source.md` 现有 `§二 录入原则` 与 `§三 结构规范` 之间。

---

## 四、范围外

- 不动 `渊海子平`/`滴天髓阐微` 的 source.md 内容
- 不做异体字/标点/避讳字等其他维度的字形规范化（如 `坂/阪`、`馀/余`、`回/囘` 避讳字、全角/半角标点等）— 仅处理"繁→简"机械映射
- 不重建 `search-index.json`（生成时自然反映）
- 不动 `generate.js`（路径推导已支持篇名变化）
- 不处理 OCR 错误、缺漏、传抄讹误（如 `兄弟/source.md` 中的 `羊玲` 应为 `羊陀`）— 不在本次范围
- 不改 `SPEC-interpretation.md` 现有内容；新解读按 source.md 现状引用即可

---

## 五、验证

| 验证项 | 方法 | 通过标准 |
|--------|------|---------|
| 繁简转换无遗漏 | 校验脚本扫描所有 source.md，列出未转换的繁体字符 | 高频繁体字（数、体、学...）剩余数为 0 |
| 歧义字已复核 | 抽样 10 篇 + 全量关键词搜索 | 出现 復/像/干/后/台/余/里/几 处均已注明判定 |
| 目录与 catalog 一致 | 校验脚本比较 catalog.md 表格与实际目录 | 完全一致 |
| 标题与文件首行一致 | 校验脚本比较 `articles/{篇名}/` 与 `source.md` 第一行 `# 标题` | 完全一致 |
| git 可追溯 | 单次 commit 完成整本书 | commit message 含 `feat(紫微斗数全书): 繁转简规范化` |

---

## 六、风险与权衡

| 风险 | 缓解 |
|------|------|
| 繁→简 歧义导致语义走样（例 `後` 误转 `后`） | 歧义字清单 + 人工/辅助 LLM 语境复核 |
| 目录 `git mv` 链路混乱 | 全部走 `git mv`；commit message 注明 |
| 渊海/滴天 不动内容产生"默认=可改"歧义 | 显式标注 `字形策略：原文照录` |
| 现有 source.md 中已有人工简体录入（如 `兄弟`），转换后无法对齐"原印本繁简" | 已与用户确认"信任现有 source.md"为底本 |
| 解读工作已基于繁简混用版本，新策略后解读引用需更新 | 紫微斗数全书 尚无 interpretation.md，无历史负担 |

---

## 七、变更文件清单

| 文件 | 变更类型 |
|------|---------|
| `research-dispute/SPEC-source.md` | 新增 §X 字形策略节 |
| `books/紫微斗数全书/catalog.md` | 元信息加 `字形策略：简体规范化`；篇名表更新 |
| `books/紫微斗数全书/articles/**/source.md` | 100+ 文件繁→简转换 |
| `books/紫微斗数全书/articles/**` | 100+ 目录 `git mv` 重命名 |
| `books/渊海子平/catalog.md` | 元信息加 `字形策略：原文照录` |
| `books/滴天髓阐微/catalog.md` | 元信息加 `字形策略：原文照录` |
| `scripts/check-fanti-residue.{js,py}` | 新增校验脚本（暂存根目录） |

---

_本设计文档为 SPEC 变更的入口；通过后将进入 writing-plans 阶段产出实施计划。_
