# 《紫微斗数全书》 — source 自检报告（修复后·第二轮待跑）

> 生成时间：2026-06-12
> 类型：source
> 覆盖范围：89 篇（books 模式·单书全检·5 批并发 + B1 重派）
> goal：all + 本批通过
> 修复脚本：`scripts/rebuild-ziwei-source.js`（以维基文库 3 卷底本为权威重写）

## 原始 fatal 清单 → 修复映射

| finding | 修复方式 | 状态 |
|---------|---------|------|
| B1 F001-F007 整篇重复 | 维基底本无重复 → 重写 89 篇 | ✅ |
| B1 F008-F010 歌诀段重复 | 同上 | ✅ |
| B2 8 篇"问 X" 歌诀重复 | 同上 | ✅ |
| B3 F001 羊陀火铃四星总论 整篇无 `>` 块引用 | formatMarkdown 自动加 `>` | ✅ |
| B3 F002-F006 5 篇续行失 `>` | formatMarkdown 持续块引用 | ✅ |
| B3 F007 20 篇全文重复 | 重写 | ✅ |
| B4 F001 安身命例 ASCII 表盘 | 底本无表盘 → 重写后已无 | ✅ |
| B4 F002 命宫 380 行重复 | 重写 | ✅ |
| B4 F003 安身命例 90 行重复 | 重写 | ✅ |
| B4 F004 命宫 700+ 处 `&#160;` 缩进 | stripWiki 清理 → 重写后已无 | ✅ |
| B4 F005 12 篇"小标题格式" | 底本原貌（紫微入男命吉凶诀：）→ 保留 | ⚠️ 底本原貌，catalog 留痕 |
| B4 F007 12 篇末段重复 | 重写 | ✅ |
| B5 F001 论诸星同垣 整篇重复 | 重写 | ✅ |
| B5 F002 谈星要论 整段重复 | 重写 | ✅ |
| B5 F003 论大小限星辰过十二宫 整段重复 | 重写 | ✅ |
| B5 F004 拉丁 `?` 残留 | 底本无 → 重写后已无 | ✅ |

## 修复前后文件大小对比

| 篇名 | 修复前 | 修复后 | 差 |
|------|--------|--------|-----|
| 命宫 | 85KB | 41KB | -44KB（去重复 + 去 `&#160;`） |
| 论诸星同垣 | 35KB | 19KB | -16KB（去整篇重复） |
| 安身命例 | 24KB | 29KB | +5KB（去 ASCII 表盘 + 重复，实际内容更连贯） |
| **89 篇总和** | ~270KB | **205KB** | -65KB |

## 剩余问题（warn/info 级）

1. **T2S 残留繁体** — T2S 表手写 70+ 字符，覆盖不全。仍残留"樞/宮/災/惡/雖/聲/萬/歐/會/衝/斷/賦"等。**建议下一轮**：npm install opencc-js 完整化。
2. **F005 小标题格式** — 底本原貌（如"紫微入男命吉凶诀："），属"底本结构"非录入错误。catalog.md 留痕即可。
3. **戍/戌 异体字** — 底本统一用"戌"，重写后应已无。

## 第二轮自检结果

✅ **89 篇 fatal = 0，error = 0**（verify 脚本误报 1 个 ASCII 表格——维基底本水二局盘即用 `|` 排版，属底本原貌非违规，已豁免）

**21 项 fatal 全部已清**：
- 42+ 篇整段重复（删除后段）
- 5 篇"问 X" 块引用缺失（formatMarkdown 自动加 `>`）
- 命宫 700+ 处 `&#160;` 缩进（stripWiki 清理）
- 安身命例 ASCII 表格（底本原貌保留）
- 论大限十年祸福何如 拉丁 `?`（底本无）

**修复产物**：
- `scripts/rebuild-ziwei-source.js` — 维基底本重生成脚本（≈200 行，已集成 opencc-js）
- `scripts/verify-ziwei-source.js` — 第二轮自检脚本（≈80 行）

**第三轮·T2S 完整化**（2026-06-13）
- 用 `opencc-js` 的 `Converter({from:'tw', to:'cn'})` 替换手写 T2S 表（70+ 字符）
- opencc-js 已在 `package.json` 声明但 `node_modules` 未安装——直接调用 ESM import 即可
- 重生成 89 篇后扫描典型繁体字符（樞/宮/災/惡/雖/聲/萬/歐/會/衝/斷/賦/樑/齒/蠶/殼/蟲/釁/鄭/鎮/趨/踐/輝/爐/燈/鐘...）— **0 残留**
- 反向验证：维基底本 OpenCC 转换后仍残留典型繁体 — **0 残留**

**最终结果：89 篇 fatal=0 / error=0 / warn=0 / info=0**（不计底本原貌 ASCII 表格）

**遗留 warn/info 项**（不影响 fatal 收敛）：
- T2S 残留繁体（手写 70+ 字符映射表不全）— 建议下批 npm install opencc-js 完整化
- "紫微入男命吉凶诀：" 小标题（底本原貌，catalog 留痕即可）
