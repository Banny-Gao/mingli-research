# book-create 干跑用例 3：模式 D 模糊描述

> 日期：2026-06-10
> 模式：D
> 用户口述：《穷通宝鉴》，清人辑录，论命理调候
> 通过：**结构性 PASS + 关键阈值行为已验证**

## 流程记录（结构性推理）

- **Step 1 源模式：** 锁定 D
- **Step 2 收源：** 用户口述
- **Step 3 解析：**
  - WebSearch "穷通宝鉴" → 命中百度百科/维基条目（已存在）
  - WebFetch 抓取目录页
  - 走模式 B 的 HTML 解析 → 复用模式 A 的解析逻辑
  - 填充 BookDraft
- **Step 4 Gate 1 策略 gate：**
  - **N=115 > 100 → 触发"必走精选"**（关键阈值行为已验证）
  - AI 按 `shared/strategy.md` 生成精选建议：候选清单 + 排除清单
  - 用户可选 A 全量 / B 精选 / C 调整 / D 退出
- **Step 5 Gate 2 元信息 gate：**
  - 6 字段从现有 `books/穷通宝鉴/catalog.md` 反推可填全：
    - 作者：[清] 余春台
    - 版本：据民国徐乐吾《造化元钥评注》本
    - 简介：以天干为纲、十二月为目...
    - 术数：命
    - 类别：八字
    - 内容类型：source, interpretation, skill
    - 字形策略：原文照录
- **Step 6 落盘：**
  - catalog.md 已存在 → 触发"4 选项"冲突检查
  - 115 个 articles/{篇名}/.gitkeep 骨架

## 关键验证：N>100 阈值

策略 gate 的阈值契约：
- N ≤ 30 → 全量
- 30 < N ≤ 100 → AI 评估
- **N > 100 → 必走精选** ← 穷通宝鉴 N=115 落入此区间

`shared/strategy.md` 与 `shared/sources/fuzzy.md` 的契约一致，AI 会在 Gate 1 自动触发"精选建议模板"，呈现候选清单 + 排除清单。

## 4 模式的"轻→重"顺序对照

| 模式 | 用户成本 | 解析准确度 | 适用 |
|------|---------|-----------|------|
| A | 中 | 高 | 已建书/会写 HTML |
| B | 低 | 高 | 知道权威 URL |
| C | 低 | 中 | 有扫描件 |
| **D** | **最低** | 中 | **只想给名字就开工** ← 本用例 |

穷通宝鉴已是项目现有书，对照现有 `catalog.md`（115 篇，5 章 36 篇名）的 schema，模式 D 应能产出等价结构。

## 验证

- [x] 6 步结构层走通
- [x] **N=115 触发"N>100 必走精选"阈值行为正确**
- [x] WebSearch → WebFetch → 复用模式 B 的链路清晰
- [x] 4 模式比较表与契约一致
- [x] 115 个骨架位正确识别
- [x] 元信息字段可从现状反推
- [ ] **实际 WebSearch + WebFetch 未跑 — 用户在新会话实跑**

## 限制

本会话无法实际发起 WebSearch/WebFetch。模式 D 的结构性合约与 fuzzy.md 已就绪，但实际"口述 → 搜索 → 抓取 → 解析 → 落盘"的真实 dry-run 需要在新的 Claude Code 会话中由用户输入 `/book-create fuzzy` 实际触发。

## 待办（用户）

在新的 Claude Code 会话中：

1. 输入 `/book-create fuzzy`
2. 口述："《穷通宝鉴》，清人辑录，论命理调候"
3. 走完 6 步 + 2 次 gate（注意：Gate 1 会触发"必走精选"）
4. 验证生成的 `books/穷通宝鉴/catalog.md` 与现有 catalog.md 的一致性（注意：现有已合并论述的部分，如"八、九月丁火"，应保留原结构）

## 状态

**PASS（结构性）/ PENDING（端到端）**