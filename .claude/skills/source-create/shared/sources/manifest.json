{
  "version": 1,
  "_root": null,
  "_root_comment": "项目根解析：null 走 process.cwd()；传相对 manifest 自身目录的路径可显式指定（如 '../../../../..'）",
  "description": "source-create 录入源映射表（manifest 是真源；shared/sources/ 副本由 scripts/ingest-skill-sources.js 按本表生成）",
  "sources": [
    {
      "id": "SPEC-source",
      "from": "research-dispute/SPEC-source.md",
      "to": "SPEC-source.md",
      "kind": "markdown",
      "required": true,
      "comment": "原文录入格式与原则的唯一真源；本副本由 ingest 自动生成"
    },
    {
      "id": "general",
      "from": "research-dispute/general.md",
      "to": "general.md",
      "kind": "markdown",
      "required": true,
      "comment": "通用红线 + 学术语体的唯一真源；本副本由 ingest 自动生成"
    },
    {
      "id": "self-check-fingerprint",
      "from": "scripts/self-check-fingerprint.py",
      "to": "scripts/self-check-fingerprint.py",
      "kind": "script",
      "required": true,
      "comment": "spec-bundles.md 指纹校验命令依赖此脚本；封装性要求副本自带"
    }
  ]
}