# 步骤 S3：改动分析

## 分析命令

```bash
git status --porcelain
git diff --staged --stat
git diff --staged --no-color
```

## 解析维度

- 改动文件类型
- 改动范围
- 改动内容
- 关联模块

## 改动卡片

```
┌─────────────────────────────────────────────────────────────┐
│  改动文件列表                                               │
├─────────────────────────────────────────────────────────────┤
│  1. src/views/user/index.vue（修改）                       │
│  2. src/views/user/api/types.d.ts（新增）                 │
│  3. src/styles/user.less（修改）                         │
│                                                             │
│  改动类型：代码修改 + 样式调整                            │
├─────────────────────────────────────────────────────────────┤
│  [继续]                                                    │
└─────────────────────────────────────────────────────────────┘
```
