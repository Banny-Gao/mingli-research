---
name: enyuan
displayName: 恩怨关系分析器
type: analysis
input: 八字四柱
output: 喜忌关系分析报告（喜神距离 + 媒的存在 + 离间风险 + 邂逅/牵合机会）
description: 基于滴天髓恩怨篇的喜忌牵合理论，分析日主与喜神之间是否存在媒（中介）的连接、是否被忌神离间，以及是否有邂逅相逢或私情牵合的特殊喜神获取方式
---

## 功能

在收到八字命局后，以"恩（喜神）"和"怨（忌神）"的视角重新审视命局中的五行关系，重点分析喜神与日主之间的连接状态。不同于通用的用神分析（只判断用什么），本技能判断"用神能发挥多大作用"——即喜神是否被有效连接（有媒）、是否被隔断（离间）、以及是否有意外或暗中的喜神获取方式（邂逅/牵合）。此技能应在完成基础用神判定之后调用，作为更深入的喜忌关系分析。

## 输入

- **四柱八字**：年柱、月柱、日柱、时柱的天干地支（字符串）
- **用神判定结果**：来自通用用神分析或知命篇顺逆之机分析的输出（可选，未提供时本技能先自行判定）
- **喜神/忌神清单**：可选，未提供时本技能用神力顺应判定

## 处理逻辑

1. **确定喜神与忌神**
   a. 先判定日主旺衰，确定用神
   b. 用神力顺应（生扶用神者为喜神、克耗用神者为忌神）列出完整的喜神和忌神清单
   c. **依据**：恩怨篇【任氏曰】"恩怨者，喜忌也"

2. **距离分析**
   a. 标记每个喜神与日主之间的柱位距离（年柱最远、月柱中等、时柱较近、日支最近）
   b. 标记每个忌神与日主的距离
   c. 应用原则："可憎之神，远之为妙；可爱之神，近之尤切"
   d. 若喜神近而忌神远 → 恩大于怨（吉）
   e. 若忌神近而喜神远 → 怨大于恩（凶）
   f. **依据**：恩怨篇【原注】"可憎之神，远之为妙；可爱之神，近之尤切"

3. **媒的判断（喜神连接通路分析）**
   a. 对于每个远距离的喜神，检查有无"媒"（中介干支）与喜神相连：
   - 是否存在三合、六合、相生关系的干支连接喜神与日主
   - 是否存在可以合化忌神从而解放喜神的干支
     b. 若存在媒 → 标记为"有媒连接，远喜实近"
     c. 若不存在媒 → 标记为"无媒连接，远喜难用"
     d. **依据**：恩怨篇【原注】"喜神合神，两情相通，又有人引用生化，如有媒矣"

4. **离间风险分析**
   a. 检查日主与喜神之间是否有忌神或闲神的阻断
   b. 检查喜神是否被合化为忌神
   c. 检查喜神是否与闲神联合助长了忌神
   d. 若存在离间 → 标记"被离间：恩化为怨"，说明具体离间方式
   e. **依据**：恩怨篇【原注】"合神喜神虽有情，而忌神离间，求合不得"

5. **特殊喜神连接方式检测**
   a. 邂逅相逢检测：
   - 命局中是否原本无喜神（全是闲神/忌神）
   - 是否有合化关系意外产生喜神
     b. 私情牵合检测：
   - 喜神是否存在但被隔离
   - 是否有地支会合将喜神暗中连接
     c. **依据**：恩怨篇【任氏曰】"只有闲神忌神而无喜神，得闲神忌神合化喜神，所谓邂逅相逢也"

6. **输出报告**

## 输出

```typescript
interface EnYuanReport {
  /** 喜神与忌神清单 */
  likesAndDislikes: {
    xiShen: Array<{
      ganZhi: string
      element: string
      role: string
      distance: '近距离' | '中距离' | '远距离'
    }>
    jiShen: Array<{ ganZhi: string; element: string; role: string; distance: string }>
  }
  /** 媒的连接分析 */
  meiAnalysis: Array<{
    xiShen: string // 被连接的喜神
    meiGanZhi: string // 充当媒的干支
    connectionType: '三合' | '六合' | '相生' | '合化忌神'
    status: '有媒' | '无媒'
    description: string
  }>
  /** 离间分析 */
  liJianAnalysis: Array<{
    affectedXiShen: string // 被离间的喜神
    liJianGanZhi: string // 离间者
    liJianType: '隔绝' | '合化' | '助纣'
    status: '被离间' | '未被离间'
    description: string
  }>
  /** 特殊连接方式 */
  specialConnections: Array<{
    type: '邂逅相逢' | '私情牵合'
    originalState: string // 初始状态
    transformedState: string // 转化后的状态
    mechanism: string // 转化机制
  }>
  /** 综合评估 */
  conclusion: {
    enYuanRatio: string // 恩大于怨/怨大于恩/恩怨相当
    keyInsight: string // 关键发现
    suggestion: string // 基于恩怨分析的命理建议
  }
}
```
